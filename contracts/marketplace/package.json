{
  "name": "@nft-marketplace/marketplace-contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "forge test --watch",
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "storage:check": "forge inspect CreatorCollectionFactory storage-layout >/dev/null && forge inspect MarketplaceSettlement storage-layout >/dev/null",
    "coverage": "forge coverage --report summary",
    "gas": "forge test --gas-report",
    "typecheck": "node -e \"console.log('[marketplace-contracts] Solidity typechecking is provided by forge build')\"",
    "generate": "yarn workspace @nft-marketplace/contracts generate",
    "generate:check": "yarn workspace @nft-marketplace/contracts generate:check",
    "deploy": "node scripts/deploy.mjs",
    "verify": "node scripts/verify.mjs",
    "docker": "node -e \"console.log('[marketplace-contracts] no contract container is defined')\""
  }
}
