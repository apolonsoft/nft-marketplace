# @nft-marketplace/workers

Placeholder for independently deployable NestJS and BullMQ workers. T-040 owns runtime scaffolding. Workers may consume shared packages and domain events but must not import another application.
