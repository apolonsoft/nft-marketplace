{
  "name": "@nft-marketplace/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@nft-marketplace/contracts": "workspace:*",
    "@ponder/core": "^0.7.17",
    "hono": "^4.7.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  },
  "scripts": {
    "dev": "ponder dev",
    "build": "ponder codegen && ../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "test": "../../node_modules/.bin/vitest run",
    "lint": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "typecheck": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "generate": "ponder codegen",
    "docker": "node -e \"console.log('[indexer] placeholder: containerization belongs to T-060')\""
  }
}
