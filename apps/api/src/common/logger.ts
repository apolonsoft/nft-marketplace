import type { LoggerService } from '@nestjs/common';
import { createLogger, type StructuredLogger } from '@nft-marketplace/observability';
import { currentRequestId } from './request-context';
export class ApiLogger implements LoggerService { readonly structured: StructuredLogger; constructor(level = 'info') { this.structured = createLogger({ service: 'api', level }); } private fields(context?: string) { const requestId = currentRequestId(); return { ...(context ? { context } : {}), ...(requestId ? { requestId } : {}) }; } log(message: unknown, context?: string) { this.structured.info(String(message), this.fields(context)); } error(message: unknown, trace?: string, context?: string) { this.structured.error(String(message), { ...this.fields(context), ...(trace ? { trace } : {}) }); } warn(message: unknown, context?: string) { this.structured.warn(String(message), this.fields(context)); } debug(message: unknown, context?: string) { this.structured.debug(String(message), this.fields(context)); } }
