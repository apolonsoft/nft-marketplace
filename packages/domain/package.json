{
  "name": "@nft-marketplace/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@nft-marketplace/config": "workspace:*",
    "zod": "^3.25.76"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./schemas": {
      "types": "./dist/schemas.d.ts",
      "import": "./dist/schemas.js"
    },
    "./models": {
      "types": "./dist/models.d.ts",
      "import": "./dist/models.js"
    }
  },
  "scripts": {
    "dev": "node -e \"console.log('[domain] placeholder: implementation belongs to T-004')\"",
    "build": "../../node_modules/.bin/tsc -p tsconfig.build.json",
    "test": "../../node_modules/.bin/vitest run --config vitest.config.ts",
    "lint": "../../node_modules/.bin/eslint . --max-warnings=0",
    "lint:fix": "../../node_modules/.bin/eslint . --fix",
    "typecheck": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "generate": "node -e \"console.log('[domain] placeholder: generation belongs to later domain tasks')\"",
    "docker": "node -e \"console.log('[domain] placeholder: shared packages are not containerized')\""
  }
}
