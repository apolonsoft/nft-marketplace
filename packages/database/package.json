{
  "name": "@nft-marketplace/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "node -e \"console.log('[database] placeholder: implementation belongs to later data tasks')\"",
    "build": "node -e \"console.log('[database] placeholder: implementation belongs to later data tasks')\"",
    "test": "node -e \"console.log('[database] placeholder: tests belong to later data tasks and T-070')\"",
    "lint": "node -e \"console.log('[database] placeholder: tooling belongs to T-003')\"",
    "typecheck": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "generate": "node -e \"console.log('[database] placeholder: schema generation belongs to later data tasks')\"",
    "docker": "node -e \"console.log('[database] placeholder: shared packages are not containerized')\""
  }
}
